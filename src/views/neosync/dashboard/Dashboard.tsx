import PlaceholderPage from 'src/shared/components/PlaceholderPage'

const Dashboard = () => (
  <PlaceholderPage titleKey="pages.dashboard.title" subtitleKey="pages.dashboard.subtitle" />
)

export default Dashboard
